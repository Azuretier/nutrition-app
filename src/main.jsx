import React from 'react'
import ReactDOM from 'react-dom/client'
import WorkoutApp from '../workout-app.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <WorkoutApp />
    </React.StrictMode>,
)
